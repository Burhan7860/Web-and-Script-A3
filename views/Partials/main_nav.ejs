<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm py-3">
  <div class="container-fluid">
    <a class="navbar-brand d-flex align-items-center" href="/">
      <img src="/Content/favicon.svg" alt="Logo" width="40" height="40" class="me-2">
      <span class="fw-bold text-primary">Burhan's Tracker</span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <% if(title=="Burhan's Assignment Tracker") { %>
            <a class="nav-link active fw-bold text-primary border-bottom border-primary" href="/">
          <% } else { %>
            <a class="nav-link text-secondary" href="/">
          <% } %>
            <i class="fa-solid fa-house me-2"></i>Home</a>
        </li>
        <li class="nav-item">
          <% if(title=="Assignment Creator") { %>
            <a class="nav-link active fw-bold text-primary border-bottom border-primary" href="/editor">
          <% } else { %>
            <a class="nav-link text-secondary" href="/editor">
          <% } %>
            <i class="fa-solid fa-plus me-2"></i>Create</a>
        </li>
        <li class="nav-item">
          <% if(title=="Manage Assignment") { %>
            <a class="nav-link active fw-bold text-primary border-bottom border-primary" href="/manage">
          <% } else { %>
            <a class="nav-link text-secondary" href="/manage">
          <% } %>
            <i class="fa-solid fa-gear me-2"></i>Manage</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
